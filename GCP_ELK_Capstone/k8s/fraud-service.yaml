apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-service
  namespace: default
  labels:
    app: fraud-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fraud-service
  template:
    metadata:
      labels:
        app: fraud-service
    spec:
      tolerations:
      - key: "CriticalAddonsOnly"
        operator: "Exists"
        effect: "NoSchedule"
      containers:
      - name: fraud-service
        image: <AWS_ACCOUNT_ID>.dkr.ecr.<REGION>.amazonaws.com/fraud-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8003
        env:
        - name: SUPABASE_URL
          valueFrom:
            secretKeyRef:
              name: microservices-secrets
              key: SUPABASE_URL
        - name: SUPABASE_SERVICE_ROLE_KEY
          valueFrom:
            secretKeyRef:
              name: microservices-secrets
              key: SUPABASE_SERVICE_ROLE_KEY
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: fraud-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: fraud-service
  ports:
  - port: 8003
    targetPort: 8003
